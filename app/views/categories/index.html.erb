<% content_for(:header) do %>
  <%= back_link "/tables" %>
  <h1>Mesa <%= current_table %></h1>
<% end %>

<ul data-role="listview" data-inset="true">
  <% @categories.each do |c| %>
    <li><%= link_to c.name, products_path(id: c.id, type: c.name) %></li>
  <% end %>
</ul>
